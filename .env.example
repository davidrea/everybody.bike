# everybody.bike — Environment Variables
# Copy to .env.local and fill in values from `npx supabase start`

# Supabase (public — safe to expose in browser)
NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
NEXT_PUBLIC_SUPABASE_ANON_KEY=<anon-key-from-supabase-start>

# Supabase (server-side only — never expose to browser)
SUPABASE_SERVICE_ROLE_KEY=<service-role-key-from-supabase-start>

# WebAuthn / Passkeys
# The Relying Party ID should match your domain (localhost for dev)
NEXT_PUBLIC_WEBAUTHN_RP_ID=localhost
NEXT_PUBLIC_WEBAUTHN_RP_NAME=everybody.bike
NEXT_PUBLIC_WEBAUTHN_ORIGIN=http://localhost:3000

# Web Push (VAPID keys — generate with: npx web-push generate-vapid-keys)
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:admin@everybody.bike

# Notification dispatch (cron/webhook secret for /api/admin/notifications/dispatch)
NOTIFICATION_DISPATCH_SECRET=
